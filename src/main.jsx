import React from "react";
import ReactDOM from "react-dom/client";
import CineVerse from "./flickshare.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CineVerse />
  </React.StrictMode>
);
